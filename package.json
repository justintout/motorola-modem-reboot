{
  "name": "motorola-modem-reboot",
  "version": "0.0.1",
  "description": "Tool to programatically reboot Motorola modem",
  "main": "dist/index.js",
  "bin": {
    "reboot-modem": "./dist/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "node -e 'fs=require(\"fs\");f=fs.readFileSync(\"src/version.ts\").toString().replace(\"UNSET-VERSION\",require(\"./package.json\").version);fs.writeFileSync(\"src/version.ts\",f);'",
    "build": "tsc",
    "postbuild": "node -e 'require(\"fs\").writeFileSync(\"src/version.ts\",`//autogenerated. do not edit.\nexport const pkgVersion=\"UNSET-VERSION\";`)'",
    "preinstall": "npm i --save typescript playwright && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "github.com/justintout/motorola-modem-reboot"
  },
  "author": "Justin Tout",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.19.1",
    "typescript": "^4.5.5"
  }
}
